---
import BaseLayout from "@/layouts/BaseLayout.astro";
import KeyboardPage from "@/components/KeyboardPage.tsx";
import { I18n } from "@/i18n/utils";
import { getTitle } from "@/i18n/pageHeadings";

const title = getTitle("keyboard", I18n.defaultLocale);
---

<BaseLayout lang={I18n.defaultLocale} title={title}>
  <KeyboardPage locale={I18n.defaultLocale} client:load />
</BaseLayout>
